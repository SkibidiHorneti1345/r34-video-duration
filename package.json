{
  "name": "r34-video-duration",
  "version": "2.0.0",
  "private": true,
  "description": "Chrome extension that overlays video durations on rule34.xxx thumbnails.",
  "scripts": {
    "test": "vitest run",
    "check": "npm test && node --check content.js && node --check src/core.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "vitest": "^4.1.9"
  }
}
